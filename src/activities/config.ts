export const ActivityActionCodes = {
	CREATE: 'create',
	UPDATE: 'update',
	REMOVE: 'remove',
} as const;

export const ActivitySphereCodes = {
	TASK: 'task',
	TAG: 'tag',
	COMMENT: 'comment',
} as const;
